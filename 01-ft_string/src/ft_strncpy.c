/* ft_strncpy.c
 *
 * Copia como mucho n caracteres de src a dest.
 * Si src es mas corta, rellena hasta n con '\0'.
 *
 * OJO: si src mide n o mas, el resultado NO lleva '\0' final.
 *      Reproducelo tal cual: asi es la funcion real.
 */

char	*ft_strncpy(char *dest, char *src, int n)
{
	(void)src;
	(void)n;
	return (dest);
}
